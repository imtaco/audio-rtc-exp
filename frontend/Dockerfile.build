# Build stage
FROM node:22-slim AS builder

WORKDIR /build

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source files
COPY . .

# Build the application
RUN npm run build

# Export stage - prepare files for output
FROM scratch AS export-stage
COPY --from=builder /build/dist /
